<%= form_for appointment do |f|  %>
  <%= f.collection_select :location_id, Location.all, :id, :name, include_blank: true %>
  <%= f.collection_select :coach_id, Location.coaches.all, :id, :name, include_blank: true %>
<% end %>